(function ($, Drupal) {
  Drupal.behaviors.merlb = {
    attach: function (context, settings) {
      /////////////////

      $('#pel-builder').once().each(function (e) {

        /////////////////
      }
  };

  }) (jQuery, Drupal);
