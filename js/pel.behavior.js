(function ($, Drupal) {
  Drupal.behaviors.plb = {
    attach: function (context, settings) {
      /////////////////

      $('#pel-builder').once('plb').each(function (e) {

        /////////////////
      }
  };

  }) (jQuery, Drupal);
