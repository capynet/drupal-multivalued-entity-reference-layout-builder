(function($, Drupal) {
        Drupal.behaviors.merlb = {
            attach: function(context, settings) {
                /////////////////

                $('#merlb-builder').once().each(function(e) {

                        /////////////////
                    }
                };

            })(jQuery, Drupal);